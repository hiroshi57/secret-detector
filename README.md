# 秘密情報 検出 — 自社だけの機密語をAIに渡さない

自社固有の機密語（型番・プロジェクト名・取引先名・原価情報など）を登録し、AIに貼る文章に混ざっていないかを照合します。

## 特徴
- 実データ入力 → 実計算（LLM API不要・キー不要）
- 入力内容は自動保存、JSONで書き出し/読み込み可能
- 非エンジニア向けUI

## 使い方
`frontend/standalone.html` をブラウザで開くだけで動きます。
